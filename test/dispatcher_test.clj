(ns dispatcher-test
  (:require [clojure.spec.alpha :as s]))

; (defn first
;   [a b]
;   (prn [a b]))

; (s/fdef first
;   :args (s/cat :a :example/a :b :example/b))

; (s/form #'first)
;; => dispatcher.test/first
